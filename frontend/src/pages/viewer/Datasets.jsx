import React from 'react';
import DatasetPage from '../common/DatasetPage';

const ViewerDatasets = () => {
  return <DatasetPage role="viewer" title="Viewer Dataset Read-Only" />;
};

export default ViewerDatasets;
